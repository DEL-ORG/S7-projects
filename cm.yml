apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  REDIS_USER: "redis"
  DB_HOST: "db"
  AUTH_HOST: "auth"
  AUTH_PORT: "8080"
  WEATHER_HOST: "weather"
  WEATHER_PORT: "5001"
  
  # REDIS_USER is duplicated in your list; it's defined only once here.
